# EvoLab

EvoLab 是一个纯前端进化模拟器。项目使用 Vite + React，模拟逻辑全部在浏览器内运行，并通过 HTML Canvas 绘制世界。

## 本地运行

```bash
npm install
npm run dev
```

打开终端输出的本地地址即可查看，通常是 `http://localhost:5173`。

## 构建

```bash
npm run build
```

构建产物会输出到 `dist/`，可部署到 Vercel、Netlify 或 GitHub Pages。
